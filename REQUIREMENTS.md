# anima — 要件定義書（v0.1）

> 複数のAIを一元管理する「管制塔」と、AIとの会話が自動で貯まる「もう一つの脳みそ」を
> 1つにした macOS 常駐アプリ。

作成日: 2026-08-17
ステータス: レビュー待ち（要件確定前）
要件定義: Opus 4.8 / 実装: 低コストモデルへ委譲予定

---

## 0. この文書の使い方

- 本書は「安いモデルがそのまま実装に着手できる」粒度を目指した仕様書。
- `【決定】` = 確定した仕様。`【既定】` = 私が置いた妥当なデフォルト（変更可）。`【要確認】` = ユーザー確認が必要。
- MVP のスコープは **①管制塔 + ②承認応答**。③（Obsidian化）は Phase 2 として設計だけ残す。

---

## 1. 背景と解く問題

AIエージェント（Claude Code セッション）が増え、次の2つが痛点になっている。

1. **どのAIが何をしているか分からない。** 特に「判断待ち」はAIが黙って待つだけで気づかず、
   何十分も止まる。
2. **AIとの会話が消える。** 思考整理や判断理由を交わしても、セッションを閉じると引けなくなる。

本アプリはこれを、常駐する「管制塔」＋「もう一つの脳みそ」で解決する。

---

## 2. 全体アーキテクチャ

```
┌─────────────────────────────────────────────────────────┐
│  各 Claude Code セッション（複数・別ターミナル/プロジェクト）  │
│    hooks（PreToolUse / Notification / Stop など）           │
│        │  HTTP POST（イベント送信・承認問い合わせ）           │
└────────┼────────────────────────────────────────────────┘
         ▼
┌─────────────────────────────────────────────────────────┐
│  anima デーモン（常駐・localhost）                          │
│   - イベント受信API（どのCLIからも叩ける共通契約）           │
│   - セッション状態ストア（誰が動作中/待機/判断待ち/完了）      │
│   - 承認仲介（フックの問い合わせを保留→UIの応答を返す）        │
│   - macOSネイティブ通知（判断待ち/入力待ちで発火）            │
│   - WebSocket でUIへリアルタイム配信                        │
│   - （Phase2）会話ログ → Obsidian互換Markdown 変換・保存     │
└────────┬────────────────────────────────────────────────┘
         │  WebSocket / REST
         ▼
┌─────────────────────────────────────────────────────────┐
│  管制塔 UI（ブラウザ or メニューバー小窓）                    │
│   - 6キャラのスロット表示（動作中/待機/判断待ち/完了）        │
│   - 判断待ちカードに「許可 / 拒否」ボタン                     │
└─────────────────────────────────────────────────────────┘
```

**設計の要点**
- **UIとバックエンドを分離**。承認や状態管理の本体はデーモン側にあり、UIはブラウザでも
  ネイティブでも同じデーモンに繋がる。将来ガワだけ Tauri / Swift に載せ替え可能。
- **イベント受信APIを共通契約にする**。Claude Code は hooks から POST するアダプタ。
  他CLI（Cursor / aider 等）は将来それぞれのアダプタを足すだけで同じAPIに乗れる。【決定：拡張設計】

---

## 3. 技術スタック 【既定】

| 層 | 採用 | 理由 |
|---|---|---|
| デーモン | Node.js（TypeScript可・まずはJSでも可） | フック側はcurlで叩くだけなので言語自由。UI・JSON・WSが一言語で完結 |
| Webサーバ | Express（REST） + `ws`（WebSocket） | 軽量・情報が多く安いモデルでも実装容易 |
| UI | 素のHTML/CSS/JS（フレームワーク無し） | 依存を増やさずMVPを最速に。後でガワ差し替え可 |
| 通知 | `osascript`（`display notification`） | 追加依存ゼロ。将来 terminal-notifier でクリック追従に強化可 |
| 永続化 | JSONLイベントログ + メモリ上state | MVPは軽く。Phase2でSQLite等へ拡張可 |
| 常駐 | launchd（LaunchAgent plist） | ログイン時自動起動・常駐 |

- ポート: `localhost:4317` 【既定・変更可】。設定ファイルで上書き可能に。
- 依存は最小限（express, ws のみ想定）。

---

## 4. 機能要件

### 4.1 ①管制塔（セッション可視化）

**キャラクター/スロット**
- **常設6スロット**。各スロットは表示名・色（アバター代わり）を持つ。【決定：6人】
- 名前・色は設定ファイル（`config/roster.json`）で編集可能。【既定】
- 新しいセッションが立つと、**空いているスロットの先頭**が自動で担当に入る。【決定】
- セッション終了後、そのスロットは一定時間（例：完了表示を残す猶予）後に解放。【既定：完了は手動クリア or 30分で自動解放】
- 7つ目以降のセッションが同時に立った場合：**「スロット待ち」列に積む**。6人が埋まっている間は
  順番待ち行列に並べ、空きが出たら先頭から着任させる。【決定】

**表示する状態（ステータス）** 【決定】
| 状態 | 意味 | トリガ |
|---|---|---|
| 🟢 動作中 (working) | ツール実行・生成中 | PreToolUse / PostToolUse など活動イベント |
| 🟠 判断待ち (awaiting-approval) | 承認ボタン待ち | 承認対象ツールの PreToolUse 保留中 |
| 🔵 入力待ち (awaiting-input) | ユーザー入力/許可待ちで停止 | Notification フック |
| ⚪️ 待機/完了 (idle/done) | 応答完了・停止 | Stop フック |
| 🔴 エラー (error) | 異常終了等 | （任意・拡張） |

**各スロットのカードに表示する情報** 【既定】
- 担当キャラ名 / 色
- セッション識別（プロジェクト名 or cwd の末尾、セッションIDの短縮）
- 現在の状態バッジ
- 最新アクティビティ（例：直近のツール名や短いメッセージ）
- 経過時間（この状態になってからの時間。「判断待ち◯分」で放置検知）

**一覧のリアルタイム更新**：WebSocketでpush。ポーリング不要。

### 4.2 ②判断待ちへの応答（承認仲介）

**目的**：`rm -rf ./tmp を実行していい？` のような判断待ちを、
ターミナルに戻らずアプリのボタンで許可/拒否できる。

**フロー** 【決定】
1. Claude Code の **PreToolUse フック**（承認対象にマッチしたツールのみ）が発火。
   フックスクリプトが stdin から受け取ったツール情報（tool_name, tool_input, cwd, session_id 等）を
   デーモンの `POST /api/approvals` に送り、**同期的に待機（ロングポーリング）**する。
2. デーモンは該当セッションを「🟠判断待ち」にし、**macOSネイティブ通知**を出し、UIに承認カードを表示。
   カードには対象コマンド/ツール内容・キャラ・セッションを表示。
3. ユーザーがUIで **許可 / 拒否** を押す → デーモンが保留中の問い合わせに応答を返す。
4. フックスクリプトは受け取った結果を **Claude Code の PreToolUse フック出力仕様**で返す：
   - 許可 → `permissionDecision: "allow"`
   - 拒否 → `permissionDecision: "deny"`（理由文を添付可）
   - タイムアウト（既定120秒）→ **何も決定せず終了**し、Claude Code 標準のターミナル承認にフォールバック。
     デーモン側の保留状態はクリア。【既定：安全側フォールバック】
   > ※ PreToolUse フック出力の正確なJSONスキーマは、導入先の Claude Code バージョンに合わせて
   >   実装時に公式仕様で確認すること（`hookSpecificOutput.permissionDecision` 系）。【要確認：実装時検証】

**承認対象の決め方** 【既定】
- 設定 `config/approval.json` の matcher で対象を決める。
- **MVPデフォルト = `Bash` ツール全部**（＝コマンド実行はすべて承認対象。例の "rm -rf" が該当。シンプルで見逃しがない）。【決定】
- 将来、危険コマンドの正規表現（`rm -rf`, `git push --force` 等）で細分化できる余地を残す。

**二重承認の回避**：本アプリが承認UIになるツールについては、フックで allow/deny を返して
Claude Code 標準プロンプトを出させない。フォールバック時のみ標準プロンプトが出る。【決定】

### 4.3 ③もう一つの脳みそ（Phase 2・設計のみ）

- **Stop フック**時にセッションの会話ログを **Obsidian互換Markdown**（frontmatter付き）へ変換して
  指定Vaultフォルダに保存。
- ファイル名例：`YYYY-MM-DD_プロジェクト_セッション短縮ID.md`。
- frontmatter に日時 / プロジェクト / 担当キャラ / タグ等。会話は発言者見出し付きMarkdownで整形。
- **全文検索**：MVPでは Obsidian 側の検索に委ねる。将来デーモンに検索APIを持たせる余地。
- 会話本文の取得方法（トランスクリプトの入手経路）は Phase 2 着手時に詳細設計。【要確認：Phase2で詰める】

---

## 5. イベント受信API（共通契約）【決定：拡張の要】

すべてのアダプタ（Claude Code hooks / 将来の他CLI）はこのAPIに送る。

### 5.1 状態イベント
`POST /api/events`
```jsonc
{
  "adapter": "claude-code",         // 送信元アダプタ種別
  "session_id": "abc123",           // セッション一意ID
  "cwd": "/Users/force/Desktop/x",  // 作業ディレクトリ（プロジェクト表示に使用）
  "kind": "activity | notification | stop | start",
  "status": "working | awaiting-input | idle | done",  // 任意
  "detail": "Bash: npm test",       // 最新アクティビティの短い説明（任意）
  "ts": 1700000000                  // epoch 秒
}
```
- デーモンはこれを受けてstateを更新し、UIへWS配信、必要なら通知。

### 5.2 承認問い合わせ（同期・保留）
`POST /api/approvals`（レスポンスまでブロック＝ロングポーリング）
```jsonc
// リクエスト
{
  "adapter": "claude-code",
  "session_id": "abc123",
  "cwd": "/Users/force/Desktop/x",
  "tool_name": "Bash",
  "tool_input": { "command": "rm -rf ./tmp" },
  "timeout_ms": 120000
}
// レスポンス
{ "decision": "allow" | "deny" | "timeout", "reason": "任意の理由文" }
```

### 5.3 UI向け
- `GET /api/state` … 現在の全スロット/セッション状態のスナップショット
- `POST /api/approvals/:id/decision` … UIの許可/拒否ボタンから呼ぶ（body: `{decision, reason}`）
- `WS /ws` … 状態変化・新規承認カードをリアルタイム配信

---

## 6. Claude Code アダプタ（hooks）

`settings.json` に登録するフック群。各フックは stdin のJSONを読み、
`curl` でデーモンに POST する薄いスクリプト（Node or shell）。

| フック | 用途 | 送信先 |
|---|---|---|
| `SessionStart` 相当/最初の活動 | セッション登録・スロット割当 | `/api/events`(kind=start) |
| `PreToolUse`（matcher=Bash） | 承認仲介 | `/api/approvals`（保留） |
| `PreToolUse`/`PostToolUse`（その他） | 動作中の可視化 | `/api/events`(kind=activity) |
| `Notification` | 入力待ち/許可待ち検知 → 通知 | `/api/events`(kind=notification) |
| `Stop` | 完了 | `/api/events`(kind=stop) |

- インストーラ（`install.sh` or `anima init`）で、これらフックを
  ユーザーの Claude Code 設定へ追記する補助を用意。【既定】

---

## 7. UI 要件（管制塔画面）【既定】

- 6スロットをグリッド/リストで一覧。状態色で一目把握。
- 判断待ちは最上部に目立つ**承認カード**（コマンド全文 + 許可/拒否）。
- 各カードに経過時間。判断待ちが長いほど強調（色/アニメ）で「放置検知」。
- 完了/待機は控えめ表示。手動クリアボタン。
- 常駐性：デーモンはlaunchdで常時起動。UIはメニューバーアイコン or ブラウザから随時開く。
  **気づき**はUIを見ていなくてもネイティブ通知で担保。

---

## 8. 非機能要件 【既定】

- **ローカル完結**：外部送信なし。全て localhost。会話内容が外に出ない。
- **軽量常駐**：アイドル時CPU/メモリ最小。イベント駆動。
- **単一ユーザー・単一Mac**前提（MVP）。
- **障害耐性**：デーモン停止中はフックはタイムアウトで標準動作にフォールバック（AIを止めない）。

---

## 9. MVP 完了の定義（Acceptance）

1. 複数の Claude Code セッションを別々に立てると、管制塔にそれぞれ別スロットで現れ、
   動作中/入力待ち/完了が色で分かる。
2. あるセッションが `Bash`（例：`rm -rf ./tmp`）を実行しようとすると、
   - macOS通知が出る
   - 管制塔に承認カードが出る
   - **許可**を押すとそのセッションでコマンドが実行され、**拒否**を押すと実行されない
   - ターミナルに戻らず操作が完結する
3. デーモンを落としても、セッション側はタイムアウト後に通常どおり動作する（ハングしない）。
4. launchd でログイン時に自動常駐する。

---

## 10. 決定事項の確定ログ

- [x] **A**：7セッション目以降は **スロット待ち行列に積む**（6固定）。
- [x] **B**：承認対象デフォルトは **Bashツール全部**。
- [x] **C**：UI初期形は **ブラウザのダッシュボード**（気づきはネイティブ通知で担保）。
- [x] **D**：アプリ名は **anima** で確定。
- [ ] **E（Phase2）**：会話全文の取得経路（トランスクリプトの入手方法）は Phase 2 着手時に詰める。

---

## 11. 実装ステップ（低コストモデルへの委譲単位）

安いモデルに渡す際は、以下の単位で1つずつ実装・検証させると事故りにくい。

1. **デーモン雛形**：Express + ws 起動、`/api/state`・`/ws`・ヘルスチェック。
2. **状態ストア + イベントAPI**：`/api/events` 受信 → メモリstate更新 → WS配信。JSONL追記。
3. **UI（管制塔）最小**：`/ws` 購読して6スロットを描画、状態色。
4. **承認仲介**：`/api/approvals` 保留・`/decision`・タイムアウト。UIに承認カード＋ボタン。
5. **ネイティブ通知**：判断待ち/入力待ちで `osascript` 通知。
6. **Claude Code アダプタ**：フックスクリプト群 + 設定追記インストーラ。
7. **常駐化**：launchd plist。
8. （Phase 2）Stop → Obsidian Markdown 変換・保存。
